meta {
  name: Update Product
  type: http
  seq: 5
}

put {
  url: {{baseUrl}}/api/products/{{productId}}
  body: json
  auth: none
}

vars:pre-request {
  productId: REEMPLAZA-CON-UUID
}

body:json {
  {
    "min_quantity": 10,
    "active": true
  }
}
